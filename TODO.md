- Inspect current `index.html` CAD showcase section and existing JS (`site.js`).
- Add new horizontal CAD buttons with hover highlight + featured detail area with image.
- Wire hover to update detail area (title/tools/year + summary) and image (from `models/` folder, mapping per project).
- Wire click to open the CAD viewer for that project (`cad-view.html?id=...`).
- Remove/replace the existing “Tip” box and “v6 engine projects”/static text area so only the new hover-based detail remains.
- Add required CSS for horizontal button strip + active/hover states + layout.
- Update `site.js` to render the new components from `projects.json`.
- Test in browser with local server.

